import { ArrayValidationPipe } from './array-validation.pipe';

describe('ArrayValidationPipe', () => {
  it('should be defined', () => {
    expect(new ArrayValidationPipe()).toBeDefined();
  });
});
